# Disable Hibernate automatic schema creation
spring.jpa.hibernate.ddl-auto=none

spring.datasource.url=jdbc:mysql://103.150.240.36:3306/api
# spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=audiosn

spring.datasource.username=root

spring.datasource.password=u72nry3![7.>U)%@

logging.level.root=WARN


# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql = true

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.SQLServer2012Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
# spring.jpa.hibernate.ddl-auto = update


## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB